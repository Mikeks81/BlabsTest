
import { formatTrackTime } from 'blabtest/helpers/format-track-time';
import { module, test } from 'qunit';

module('Unit | Helper | format track time');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatTrackTime([42]);
  assert.ok(result);
});

